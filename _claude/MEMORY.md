# Memory Index

One line per memory file. Under ~150 characters per entry.
Format: `- [Title](memory/filename.md) — one-line hook`

Memory files live in `_claude/memory/`. This index is loaded at every session.

---

*(Add memory entries here as the vault accumulates knowledge about the team)*
